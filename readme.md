Command to run the program : 
```
cd "/home/ayush/OS_Assignment/CP_Phase2/" && gcc code.c utils.c globals.c -o code && "/home/ayush/OS_Assignment/CP_Phase2/"code
```

NOTE: extern in .h files means the variable exists somewhere else